<% unless user == current_user %>
  <%= link_to 'Contact', '',class: 'ep-index__info-right' %> <br>
  <% if user.friend_with? current_user %>
    <p class="ep-index__info-right">Connected</div>
  <% elsif user.invited_by? current_user %>
    <%= link_to 'Cancel request', remove_deny_friend_user_path(user), method: :put, remote: true, class: 'ep-index__info-right' %>
  <% elsif user.invited? current_user%>
    <div class="ep-connection-choice">
      <%= link_to 'Approve', approve_friend_user_path(user), method: :put, remote: true, class: 'ep-approve ep-index__info-right' %>
      &nbsp|&nbsp
      <%= link_to 'Decline', remove_deny_friend_user_path(user), method: :put, remote: true, class: 'ep-deny ep-index__info-right'%>
    </div>
  <% else %>
    <%= link_to "Connect", invite_friend_user_path(user), method: :put, class: 'ep-index__connect ep-index__info-right', remote: :true, data: {confirm: "Are you sure?"} %>
  <% end %>
<% end %>